{
  "name": "angular-states",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "npm install && cd angular-state-app && npm install",
    "start-server": "cd server && json-server db.js",
    "start-app": "cd angular-state-app && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "faker": "^4.1.0"
  }
}
